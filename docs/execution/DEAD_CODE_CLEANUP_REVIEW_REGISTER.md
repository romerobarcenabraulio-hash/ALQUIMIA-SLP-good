# Dead Code Cleanup Review Register

Fecha: 2026-06-01

## Decision

`CLEANUP REVIEW: MICRO_CLEANUP_APPLIED + STATIC_EXPORT_PURGE`

Eight files with zero textual references outside themselves or no external symbol consumers were deleted. The repository still needs a real `ts-prune` / `depcheck` / `unimported` / `knip` pass once npm registry access is available.

On 2026-06-02, the obsolete tracked static export `audit_visual_maqueta/` was removed from the workspace after reference review. It was a historical copy of `frontend/out`, not runtime code, not an active evidence folder, and not part of `/v`, `/p`, `/e`, `/admin`, backend, export ZIP/PDF, or bibliography logic.

## Why nothing was deleted

- `npx` could not download scanners because DNS resolution failed for `registry.npmjs.org`.
- Many candidates live under `frontend/src/components/simulator/**`, and `frontend/src/lib/legacyQuarantineManifest.ts` explicitly marks simulator legacy as high-risk until active client-facing imports are fully cut and routes pass.
- Some candidates are Next App Router conventions, test guardrail references, or UI utilities that local import normalization can flag incorrectly.

## Strong Review Candidates

These had zero textual references outside themselves in the local scan. They are candidates for removal only after a real scanner confirms them and typecheck/tests pass after deletion.

| File | Current signal | Deletion status | Required proof before delete |
| --- | --- | --- | --- |
| `frontend/src/components/charts/ContadorOportunidad.tsx` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/components/charts/VolumenBarChart.tsx` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/components/simulator/BenchmarkLATAM.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/CircularityBaselineCard.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/ComparadorEscenarios.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/DespliegueOperativoCaRecicladoraChart.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/FlujosSankey.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/HorizonteCircularidad.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/simulator/SelectorZM.tsx` | No textual external refs; simulator legacy | `TODO_CLEANUP_REVIEW` | Simulator quarantine cleared + visual routes pass |
| `frontend/src/components/ui/NoDataState.tsx` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/components/ui/SectionHeader.tsx` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/data/sankeyData.ts` | No external symbol consumers | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/lib/audienceModeLabel.ts` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/lib/ganttExport.ts` | No external symbol consumers | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |
| `frontend/src/lib/landingReferenceKpis.ts` | No textual external refs | `DELETED_MICRO_CLEANUP` | Typecheck after deletion |

## Deleted In This Pass

- `frontend/src/components/charts/ContadorOportunidad.tsx`
- `frontend/src/components/charts/VolumenBarChart.tsx`
- `frontend/src/components/ui/NoDataState.tsx`
- `frontend/src/components/ui/SectionHeader.tsx`
- `frontend/src/lib/audienceModeLabel.ts`
- `frontend/src/lib/landingReferenceKpis.ts`
- `frontend/src/data/sankeyData.ts`
- `frontend/src/lib/ganttExport.ts`

## Generated Or Historical Artifacts Removed

| Path | Status | Reason | Runtime impact |
| --- | --- | --- | --- |
| `audit_visual_maqueta/` | `DELETED_STATIC_EXPORT` | Historical static export copied from old `frontend/out`; only referenced by obsolete planning/audit notes. | None expected; not imported by product code. |
| `frontend/out/` | `DELETED_LOCAL_GENERATED` | Local generated Next output. | None; regenerated by build/export when needed. |
| `frontend/.next/` | `DELETED_LOCAL_GENERATED` | Local Next build/cache output with duplicated ` 2`/` 3` files. | None; regenerated by `next build` or dev server. |
| `backend/scripts/__pycache__/` | `DELETED_LOCAL_GENERATED` | Python bytecode cache. | None; regenerated by Python runtime. |
| `cursor-rules/.DS_Store` | `DELETED_LOCAL_OS_FILE` | macOS Finder metadata inside rule folder. | None. |

Deletion rule applied: the file was tracked, had only self-references or no external symbol consumers in `rg`, did not live under Next App Router conventions, and was outside the high-risk simulator quarantine block.

## Do Not Delete From Current Evidence

| File | Reason |
| --- | --- |
| `frontend/src/app/global-error.tsx` | Next App Router convention. Import graph does not apply. |
| `frontend/src/components/platform/StageWorkspace.tsx` | Referenced by quarantine/tests and related stage workspace guardrails. |
| `frontend/src/components/landing/WalkthroughArticle.tsx` | Referenced by client-facing guardrail tests. |
| `frontend/src/components/ui/Card.tsx` | Referenced by many UI files; local scan import normalization over-reported it. |
| `frontend/src/data/socialStats/index.ts` | Referenced by admin routes, scripts, middleware, and audit artifacts. |
| `frontend/src/components/simulator/Macrogeneradores.tsx` | Referenced by package/engine/export related files. |
| `frontend/src/components/simulator/ImpactoAmbiental.tsx` | Referenced by simulator stack components. |
| `frontend/src/components/simulator/ScoreViabilidad.tsx` | Referenced by recommendation components. |

## Dependency Review Notes

Potential unused package candidates must be reviewed with real `depcheck`/`knip`; do not remove from textual scan alone. In particular, `react-dom`, `postcss`, `tailwindcss`, `typescript`, `eslint`, `jsdom`, type packages, and peer dependencies can be required by framework/tooling without direct source imports.

## Safe next command when network works

```bash
cd /Users/braulioromerobarcena/Documents/alquimia-slp/frontend
npx --yes knip
npx --yes depcheck
npx --yes unimported
npx --yes ts-prune
```

Then delete only one small group at a time and run:

```bash
npm run type-check
npm test -- --run
npm run build
```
