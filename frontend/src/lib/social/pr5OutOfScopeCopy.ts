/** Fuera de alcance PR5 (producto / Auditoría). */
export const PR5_OUT_OF_SCOPE_ITEMS = [
  'Envío por correo, webhooks o integraciones server-to-server desde esta capa',
  'Persistencia de exportaciones en backend o colas',
  'Plantilla Word oficial, firma digital o documento administrativo único',
] as const
