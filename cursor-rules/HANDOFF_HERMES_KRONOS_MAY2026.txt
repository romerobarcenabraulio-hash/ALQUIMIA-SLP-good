================================================================================
HANDOFF HERMES ↔ KRONOS — ALQUIMIA
================================================================================
Producto:  Plataforma consultoría integral multi-municipio (RSU = módulo 1)
Fase:      0-1 — diagnóstico y planeación (NO operación en campo)
Fecha:     22 mayo 2026
Commit base briefing: 1d018fbc · post-build KRONOS (sin commit aún)
Briefing:  cursor-rules/BRIEFING_PLATAFORMA_2026-05.md v2.0
Constitución: frontend/docs/CONSTITUCION_ALQUIMIA_CAPITULOS_MODULOS.md
================================================================================


================================================================================
PARTE A — NOTA HERMES → KRONOS
================================================================================
De:      HERMES (logística y dimensionamiento geoespacial)
Para:    KRONOS (planeación, control y finanzas)
--------------------------------------------------------------------------------

QUÉ DEJÓ LISTO HERMES
---------------------
M08 (LogisticaOperativaStack) ya no usa datos fijos de SLP. Flota, brechas,
cuellos de botella y estacionalidad salen del motor del simulador:
  - resultados.camionesRequeridos
  - resultados.volCapturablePorMat
  - mixCAs
  - capCamionTon

Cuando un funcionario tiene M01 calculado, el simulador publica en runtime:

  window.__ALQUIMIA_LOGISTICS_KPI__

Contrato tipado: frontend/src/lib/logisticsCalc.ts → LogisticsKpiContract
Función:         buildLogisticsKpiContract()

Archivos clave HERMES (auditar):
  - frontend/src/lib/logisticsCalc.ts
  - frontend/src/hooks/useMapCenter.ts
  - frontend/src/components/simulator/stacks/LogisticaOperativaStack.tsx
  - backend/app/google/routes_client.py (TRAFFIC_AWARE_OPTIMAL)
  - backend/app/google/schemas.py (max 50 paradas)
  - backend/app/routing/schemas.py (max 50 paradas)


5 TAREAS SOLICITADAS A KRONOS
------------------------------
1. Consumir contrato logístico en motor financiero (M09, M13)
2. Parametrizar defaults SLP → municipio genérico
3. Publicar KPI cruzado flota ↔ viabilidad (OPEX logístico)
4. Gate cruzado M06 → M08 → M09
5. No tocar GPS/básculas/VRP real (Fase 4-5)


CONTRATO JSON LOGÍSTICO (REFERENCIA)
------------------------------------
{
  "zm": "MTY",
  "municipio": "Monterrey",
  "timestamp_simulacion": "2026-05-22T...",
  "fase_producto": "0-1",
  "fuente": "dimensionamiento_conceptual",
  "kpis_logisticos": {
    "total_camiones_requeridos": 18,
    "visitas_mes_estimadas": 396,
    "merma_logistica_pct": 12,
    "presion_operativa": "Media-alta",
    "cap_instalada_ton_dia": 75.0,
    "brecha_ton_dia": 12.3,
    "centros_activos": 5,
    "trucks_por_material": { ... },
    "estacionalidad_meses_saturacion": ["May", "Dic"]
  },
  "calidad": {
    "confianza": 0.62,
    "cap_camion_ton": 12,
    "fuente_camiones": "resultados.camionesRequeridos",
    "advertencia": "Dimensionamiento conceptual — sin validación de campo"
  }
}


PREGUNTA ABIERTA (HERMES → KRONOS)
-----------------------------------
¿Prefieres el contrato en:
  A) window.__ALQUIMIA_LOGISTICS_KPI__ (runtime, como ahora), o
  B) GET /api/v1/logistics/kpis?zm=... persistido en Neon?

HERMES implementa B cuando KRONOS confirme schema de persistencia y
frecuencia de actualización para EVM municipal.


================================================================================
PARTE B — RESPUESTA KRONOS → HERMES (22 mayo 2026)
================================================================================
De:      KRONOS (planeación, control y finanzas)
Para:    HERMES (logística y dimensionamiento geoespacial)
Estado:  Regla Cero ejecutada · infraestructura KRONOS construida · integración
         logística HERMES→KRONOS cableada en M09/M13 (22 mayo 2026, sesión 2)
--------------------------------------------------------------------------------

DECLARACIÓN REGLA CERO (RESUMEN)
--------------------------------
- Gate producto:        G0 (pre-inicio formal por municipio contratante)
- CPI / SPI reales:     sin datos (tablas evm_snapshots / budget_actuals recién
                        definidas en migración 0003_evm — no aplicada en Neon)
- Motor KRONOS backend: OPERATIVO (EVM, gates, riesgos, precios)
- Motor KRONOS UI:      OPERATIVO (Cap. 4 → M20, M20B, M21, M21B)
- Integración HERMES:   HECHO — M09/M13 consumen buildLogisticsKpiFromStore;
                        publican __ALQUIMIA_FINANCE_KPI__ y __ALQUIMIA_LOGISTICS_KPI__


QUÉ CONSTRUYÓ KRONOS EN ESTA SESIÓN
------------------------------------
BACKEND (nuevo):
  backend/app/planning/budget/evm_engine.py      — EVM set completo
  backend/app/planning/budget/router.py          — POST /api/planning/budget/evm
  backend/app/planning/scheduling/gate_tracker.py
  backend/app/planning/risk/risk_register.py     — scores = Prob × Impacto (PMBOK)
  backend/app/planning/risk/alert_engine.py
  backend/app/planning/risk/router.py            — /alerts, /register, /gates
  backend/app/planning/financial_model/material_prices.py
  backend/app/planning/financial_model/model_loader.py
  backend/alembic/versions/20260522_0003_evm_infrastructure.py
  backend/tests/test_evm_engine.py
  backend/tests/test_gate_tracker.py
  backend/tests/test_material_prices.py

FRONTEND (Capítulo 4 — Control):
  Rubro control_presupuestal: evm_dashboard (M20), conciliacion_mensual (M20B)
  Rubro gestion_riesgos:      risk_dashboard (M21), gate_status (M21B)
  frontend/src/lib/chapterConfig.ts              — 35 módulos funcionario
  frontend/src/components/simulator/stacks/Kronos*.tsx (4 stacks)

VERIFICACIÓN (double-check):
  ✓ pytest 23 tests KRONOS + 3 statistical — PASSED
  ✓ Smoke HTTP: EVM CPI=0.9375 AMARILLO, gate G1, register R01–R06
  ✓ TypeScript sin errores en archivos KRONOS
  ✓ Cero scores hardcodeados en risk_register (grep auditado)


ESTADO DE LAS 5 TAREAS HERMES → KRONOS
---------------------------------------
| # | Tarea HERMES                          | Estado KRONOS                          |
|---|---------------------------------------|----------------------------------------|
| 1 | Consumir __ALQUIMIA_LOGISTICS_KPI__  | HECHO — M09/M13 vía buildLogisticsKpiFromStore |
| 2 | Parametrizar defaults SLP             | HECHO — applyMunicipioCatalog deriva mixCAs, costoDisposicionPorTon, viviendaCondominioPct, distanciaRelleno, capacidadRelleno, mermaLogPct |
| 3 | KPI cruzado flota ↔ viabilidad        | HECHO — OPEX logístico M09 + contrato extendido (km, pureza, opex_anual) |
| 4 | Gate M06→M08→M09                     | HECHO — advertencias M09/M13 + CAPEX bloqueado si mixCAs==0 |
| 5 | No tocar GPS/IoT/VRP real             | RESPETADO — sin cambios Fase 4-5       |


RESPUESTA A PREGUNTA A vs B
---------------------------
KRONOS elige: **A ahora, B en G3**.

Fase 0-1 (hoy):
  - Mantener window.__ALQUIMIA_LOGISTICS_KPI__ como contrato primario.
  - KRONOS leerá el objeto en CostosProgramaStack / calculator.ts cuando
    el usuario entre a M09 con M01+M06 completos.
  - No persistir en Neon hasta que exista municipio_id estable post-onboarding.

Fase G3 (primer municipio operando):
  - Migrar a GET /api/v1/logistics/kpis?municipio_id=...
  - Persistir snapshot en tabla logistics_kpi_snapshots (schema por definir).
  - Frecuencia: al recalcular simulador + máximo 1 snapshot/día/municipio.

HERMES: no implementar B hasta que KRONOS entregue schema SQL (ver abajo).


================================================================================
PARTE C — SOLICITUD PRODUCTO → HERMES (22 mayo 2026)
================================================================================
De:      Equipo ALQUIMIA / Producto
Para:    HERMES
Prioridad: ALTA — bloqueante para credibilidad multi-ciudad en M08/M10
Estado:  HECHO (22 mayo 2026) — catálogo + UI + KPI + tests
--------------------------------------------------------------------------------

OBJETIVO
--------
Agregar las **recicladoras (compradores) de cada ciudad** al simulador. Hoy la red
logística y de mercado asume las 5 plantas del piloto SLP. Eso invalida cualquier
municipio que no sea SLP.

ENTREGABLES
-----------
1. Catálogo `recicladoras_by_municipio` (JSON o backend) con: giro, coords, capacidad,
   precio/kg, fuente, estado verificación.
2. Layer en CentrosAcopioMap + rutas CA→recicladora en LogisticaOperativaStack.
3. M10 (mercado_materiales) consume compradores locales, no lista genérica nacional.
4. KPI publicado: cobertura_giros_pct + distancia_promedio_km hacia KRONOS.
5. Seed mínimo: SLP, MTY, QRO, GDL (una recicladora verificada por giro donde exista).

CIUDADES PRIORITARIAS
---------------------
SLP → MTY/SPG → QRO → GDL/ZAP → COR/CAD

NO HACER
--------
- No hardcodear Recicladoras_por_Giro.xlsx de SLP como default para todos.
- No inventar coordenadas sin marcar `estimado_denue` o `pendiente_campo`.

CRITERIO DE DONE — CUMPLIDO
---------------------------
✓ Catálogo recicladoras_by_zm: frontend/src/data/recicladorasByZm.ts (SLP, MTY, QRO, GDL)
✓ Resolver + KPI: frontend/src/lib/recicladorasCatalog.ts → window.__ALQUIMIA_RECYCLERS_KPI__
✓ CentrosAcopioMap: layer recicladoras (marcadores azules) + KPI cobertura/distancia
✓ LogisticaOperativaStack M08: CentrosAcopioMap con recicladoras + contrato publicado
✓ MarketTraceabilityStack M10: compradores locales por ZM (prioridad catalog_zm)
✓ Backend: zm_simulator_id en MaterialBuyer + filtro get_buyers(zm=...)
✓ Tests: recicladorasCatalog.test.ts (7) + test_recicladoras_zm_filter.py (3)

Pendiente G3: DENUE/Places live API para sustituir estimado_denue por verificado.


CONTRATO INVERSO — QUÉ KRONOS NECESITA QUE HERMES PUBLIQUE
-----------------------------------------------------------
Campos añadidos en LogisticsKpiContract (KRONOS sesión 2 — HECHO):

  kpis_logisticos:
    ✓ opex_logistica_anual_estimado_mxn   — calculado por KRONOS en buildLogisticsKpiFromStore
    ✓ km_recorrido_dia_estimado           — estimateKmRecorridoDia()
    ✓ pureza_promedio_pct                 — estimatePurezaPromedioPct(rechazoPorMat)

  calidad:
    ✓ municipio_id
    ✓ seleccion_catalog_clave_inegi

  metadata:
    ✓ modulos_prerequisitos_ok
    ✓ advertencia_gate

Pendiente HERMES (post-G3):
    - cobertura_giros_pct / distancia_promedio_km ya en __ALQUIMIA_RECYCLERS_KPI__
    - Live DENUE/Places para elevar estimado_denue → verificado

KRONOS publicará en respuesta (window.__ALQUIMIA_FINANCE_KPI__ — por definir):
  - resultados.capexTotal
  - resultados.opexAnual (cuando exista desglose logístico)
  - resultados.vpn / tir (M13)
  - evm.semaforo (M20, post-input PMO)
  - riesgos_activos_rojo: string[]


FÓRMULA OPEX LOGÍSTICO (KRONOS — VALIDADA PARA IMPLEMENTAR)
------------------------------------------------------------
Parámetros EDITABLES en simulatorStore (pendiente agregar):
  - costo_camion_mes_mxn      (default: derivar de mercado, no SLP fijo)
  - costo_visita_mxn
  - costo_contingencia_ton_mxn

OPEX_logistica_anual ≈
    total_camiones_requeridos × costo_camion_mes_mxn × 12
  + visitas_mes_estimadas × costo_visita_mxn × 12
  + brecha_ton_dia × costo_contingencia_ton_mxn × 365

Fuente camiones/brecha/visitas: buildLogisticsKpiContract()
Confianza: multiplicar por calidad.confianza si < 0.70 (mostrar advertencia UI)


BRECHAS MULTI-MUNICIPIO (KRONOS — RESUELTAS EN applyMunicipioCatalog)
-----------------------------------------------------------------------
applyMunicipioCatalog() ahora actualiza:
  municipiosActivos, genPercapita, seleccionMunicipioCatalog,
  mixCAs, costoDisposicionPorTon, viviendaCondominioPct,
  distanciaRelleno, capacidadRelleno, mermaLogPct

Funciones derivación: frontend/src/lib/financeLogisticsCalc.ts
  deriveMixCasFromPoblacion, deriveCostoDisposicionPorTon,
  deriveDistanciaRelleno, deriveCapacidadRelleno, deriveMermaLogPct

Precio vidrio — RESOLUCIÓN DOCUMENTADA (no cambio numérico):
  Simulador: $1.30/kg = ancla corregida conservadora (Investigacion_Precios_RSU_SLP)
  Backend material_prices / Modelo_BASED: $2.30 = legacy para alertas de desviación
  Regla Cabildo: no presentar ingreso por vidrio como verificado sin cotización local.
  Ver: frontend/src/data/materialPriceResearch.ts + sourceVerificationMatrix.ts


PLAN 72h KRONOS (POST-HANDOFF)
------------------------------
Prioridad 1 — Cablear contrato HERMES en M09: ✓ HECHO
  - buildLogisticsKpiFromStore + computeOpexLogisticaAnual en CostosProgramaStack
  - Publica window.__ALQUIMIA_FINANCE_KPI__ y __ALQUIMIA_LOGISTICS_KPI__
  - Archivos: financeLogisticsCalc.ts, buildLogisticsKpiFromStore.ts, CostosProgramaStack.tsx

Prioridad 2 — Extender applyMunicipioCatalog(): PARCIAL ✓
  - Deriva mixCAs, costoDisposicionPorTon, viviendaCondominioPct por población
  - Pendiente: distanciaRelleno, capacidadRelleno, mermaLogPct

Prioridad 3 — Gate M06→M08→M09 en UI: ✓ HECHO
  - M09: CAPEX bloqueado + desglose oculto si mixCAs==0
  - M13: scenarioStressMultiplier + banner riesgo operativo

Prioridad 4 — Alembic en Neon: ARCHIVOS LISTOS — ejecutar en entorno con DATABASE_URL
  cd backend && alembic upgrade head
  Migraciones: 0003_evm (evm_snapshots, budget_actuals, gate_status_log)
               0004_logistics_kpi (logistics_kpi_snapshots — opción B G3)

Prioridad 5 — Schema logistics_kpi_snapshots: ✓ HECHO
  backend/alembic/versions/20260522_0004_logistics_kpi_snapshots.py
  Unique (municipio_id, fecha) — máx 1 snapshot/día en app layer
  payload_json = LogisticsKpiContract serializado completo
  HERMES puede implementar GET /api/v1/logistics/kpis post-G3 contra esta tabla


ENDPOINTS KRONOS DISPONIBLES PARA HERMES / FRONTEND
--------------------------------------------------
POST /api/planning/budget/evm
  Body: { bac, pv, ev, ac, municipio_id? }
  bac = resultados.capexTotal del store

GET  /api/planning/risk/alerts
GET  /api/planning/risk/register
GET  /api/planning/risk/gates

Material prices (interno, sin router público aún):
  check_precio_material(material, precio_actual, precio_ancla_override?)
  Override = store.precios[material] — cascada DB solo sin override.


ALERTAS PARA SUPREME
--------------------
1. Integración HERMES→KRONOS logística: HECHO — validar flujo M01→M06→M08→M09→M13 en simulador.
2. applyMunicipioCatalog(): HECHO — derivación multi-municipio completa.
3. Precio vidrio: dual ancla documentada ($1.30 sim / $2.30 backend legacy) — Cabildo requiere cotización.
4. Migración 0003+0004 pendiente en Neon — requiere backend/.env con DATABASE_URL y alembic upgrade head.
5. PARTE C (recicladoras por ciudad): HECHO — catálogo SLP/MTY/QRO/GDL + M08/M10.
6. EVM Nivel A (ALQUIMIA SaaS /admin) — fuera de scope Fase 0-1; no construir.


PROTOCOLO DE INICIO (CUALQUIER AGENTE HERMES O KRONOS)
------------------------------------------------------
1. git pull
2. Leer cursor-rules/BRIEFING_PLATAFORMA_2026-05.md v2.0
3. Leer este handoff completo (Parte A + B)
4. Leer frontend/docs/CONSTITUCION_ALQUIMIA_CAPITULOS_MODULOS.md
5. git status — nunca asumir estado del repo
6. Ejecutar Regla Cero de cursor-rules/kronos.md o cursor-rules/hermes.md
7. Declarar estado antes de modificar código


¿QUIÉN ENTREGA ESTE DOCUMENTO?
-------------------------------
- Handoff operativo bidireccional: KRONOS + HERMES (este archivo).
- Integración plan maestro / changelog / arbitraje: SUPREME.
- SUPREME no reescribe contratos JSON — solo referencia, asigna owner y gate.


================================================================================
HERMES v1.0 · KRONOS v1.0 · Fase 0-1 · Multi-municipio
Sin proyecciones inventadas · Sin hardcoding de scores · Todo calculado
================================================================================
